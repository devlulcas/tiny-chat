{
  "name": "server",
  "version": "1.0.0",
  "description": "The server for the chat app",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup",
    "dev": "tsup --watch & node --watch .",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@fastify/one-line-logger": "^1.3.0",
    "@types/node": "^20.12.7",
    "@types/ws": "^8.5.10",
    "tsup": "^8.0.2"
  },
  "dependencies": {
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/websocket": "^10.0.1",
    "fastify": "^4.26.2"
  }
}
